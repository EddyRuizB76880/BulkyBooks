{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  //Make sure to name this block exactly ConnectionStrings, or else,
  //the program.cs file won't be able to find the DefaultConnection field.
  "ConnectionStrings": { //From left to right:
    //Server name to find          
    //DB name, if no db is found, then a new one under this name will be created.
    "DefaultConnection": "Server=localhost\\MSSQLSERVER01;Database=Bulky;Trusted_Connection=True;TrustServerCertificate=True"
  }
}
